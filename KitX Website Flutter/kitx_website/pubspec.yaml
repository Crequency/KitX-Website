name: kitx_website
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.5 # 路由管理
  f_logs: ^2.0.1 # 日志管理
  community_material_icon: ^5.9.55 # Material Design Icons 社区贡献
  url_launcher: ^6.1.10 # 打开链接
  package_info_plus: ^3.0.3 # 获取包信息
  # flutter_svg: ^2.0.2 # SVG 图片
  # flutter_markdown: ^0.6.14 # Markdown 渲染
  # http: ^0.13.5 # HTTP 请求

dev_dependencies:
  flutter_test:
    sdk: flutter

  # flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
